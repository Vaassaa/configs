% sections/resize.tex — entering the "resize" mode. The mode's own
% bindings (hjkl / arrows to shrink+grow, Return/Esc to exit) are
% summarised in the group's note rather than as separate rows.
\sr{Mod,r}{enter resize mode}
