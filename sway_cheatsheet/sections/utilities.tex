% sections/utilities.tex — media/volume/brightness keys (XF86Audio*,
% XF86MonBrightness*) and the screenshot binding. Uses \srx instead of
% \sr because the left cell needs two keycaps or a shortened label
% rather than a plain Mod-combo.
\srx{\key{Mute}}{toggle speaker mute}
\srx{\key{Vol -} \key{Vol +}}{volume down / up (5\%)}
\srx{\key{Mic}}{toggle mic mute}
\srx{\key{Play}}{play / pause playback}
\srx{\key{Prev} \key{Next}}{previous / next track}
\srx{\key{Stop}}{stop playback}
\srx{\key{Bright -} \key{Bright +}}{brightness down / up (5\%)}
\srx{\key{PrtSc}}{screenshot (grim)}
